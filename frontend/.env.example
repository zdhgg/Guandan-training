VITE_PORT=3005
VITE_PROXY_TARGET=http://localhost:8005

# Optional: if you want the frontend to call the backend directly instead of using the Vite proxy.
VITE_API_BASE_URL=

# Optional: the player id used by the UI in battle/training modes.
VITE_PLAYER_ID=player1
